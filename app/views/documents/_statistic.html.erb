<div class="table-responsive">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th class="text-center"><%= t "history.stt" %></th>
        <th class="text-center"><%= t "user.avatar" %></th>
        <th class="text-center"><%= t "user.name" %></th>
        <th class="text-center"><%= t "history.name_action" %></th>
        <th class="text-center"><%= t "static.number" %></th>
      </tr>
    </thead>
      <tbody>
        <% @list_statics.each_with_index do |history, index| %>
          <tr>
            <td class="text-center"><%= index + 1 %></td>
            <td class="image-history"><%= image_tag history.user.avatar.url, class: "img-responsive" %></td>
            <td><%= link_to history.user.name, edit_user_registration_path %></td>
            <td><%= history.name_action %></td>
            <td><%= history.number %></td>
          </tr>
        <% end %>
      </tbody>
  </table>
  <%= will_paginate @list_statics %>
</div>
