<div class="container-stories">
  <div class="col-md-8">
    <div class="page-header" id="stories">
      <h1 class="text-primary text-center"><%= t "document.list_documents" %></h1>
    </div>
    <div class="col-md-6" style="margin-bottom: 10px;">
      <div class="row">
        <div class="col-md-6">
          <%= image_tag @document.image.url, class: "img-responsive"%>
        </div>
        <div class="col-md-6">
          <h3><%= @document.name %></h3>
          <br />
          <p class="text-justify"><%= @document.description %></p>
          <br />
          <% if !check_owner?(@document.owner.id) %>
            <%= link_to t("download"), "#", class: "button-more-success btn" %>
          <% else %>
          <% end %>
          <br />
        </div>
      </div>
    </div>
  </div>
</div>
