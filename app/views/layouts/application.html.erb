<!DOCTYPE html>
<html>
  <head>
    <%= render "layouts/rails_default" %>
  </head>

  <body data-locale="<%= I18n.locale %>">
    <%= render "layouts/navbar" %>
    <section id="blog-section" >
      <div class="container">
        <%= render "shared/infor_messages" %>
        <div class="row">
          <%= yield %>
        </div>
      </div>
    </section>
    <%= content_for :javascript %>
  </body>
</html>
